<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.mapper.CcStockMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.study.entity.CcStock">
        <id column="cc_id" property="ccId" />
        <result column="wh_id" property="whId" />
        <result column="g_id" property="gId" />
        <result column="cc_num" property="ccNum" />
    </resultMap>
    <update id="updateNum">
        update cc_stock set cc_num =#{ccNum} where cc_id =#{ccId}
    </update>
    <select id="selectByWidAndGid" resultMap="BaseResultMap">
        select  cc_id, wh_id, g_id, cc_num  from cc_stock    where g_id  = #{gId}   and   wh_id  =#{whId}
    </select>

</mapper>
